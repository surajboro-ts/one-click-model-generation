/**
 * Platform Version
 *
 * Single source of truth for the Radiant Play platform version.
 * Updated only by the main maintainer when releasing to the main repo.
 * Designers read this as a read-only indicator — it tells them which
 * version of Radiant Play they are running, not anything about their prototypes.
 *
 * Format: YY.M.Ws — Year.Month.Week + sub-release letter (e.g. 26.4.1b)
 * Update this when pushing a release to main/galaxy.
 */
export const PLATFORM_VERSION = '26.5.2e';
