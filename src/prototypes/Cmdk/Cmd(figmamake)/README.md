
  # Command + K  (make)

  This is a code bundle for Command + K  (make). The original project is available at https://www.figma.com/design/4LhLxU0siSUj4CDrPzjsFh/Command---K---make-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  